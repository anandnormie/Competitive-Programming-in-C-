long long stringtointeger(string s)
{
    reverse(s.begin(),s.end());
    long long num=0;
    for(ll i=0;i<s.size();i++)
    {
        ll a=s[i]-'0';
        num+=pow(10,i)*a;
    }
    return num;
}


// just copy this function above your int main function to use it...

